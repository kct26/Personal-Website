/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // If you deploy to GitHub Pages instead of Vercel, GitHub Pages can only
  // serve static files — uncomment the line below to make `next build`
  // output a static site into an `out/` folder instead of requiring a
  // Node server. See README.md for the full deploy walkthrough either way.
  // output: 'export',
};

export default nextConfig;
